var FormEditorPreconfig = window.FormEditorPreconfig || {
    description: "This object must be create in global before FormEditor's modules is loaded, it mean if you see that line, default config will be applied",
    extendAttributes:{}
};
export default FormEditorPreconfig;